pipeline {
    agent any

    stages {
        stage('Execute MySQL Queries') {
            steps {
                sh 'mysql -u root -proot < scripts/schema.sql'
            }
        }
    }
}
